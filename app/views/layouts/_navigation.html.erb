<!-- Navigation-->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container px-4 px-lg-5">
    <%= link_to 'Kobazon',items_path,class:'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
        <li class="nav-item"><%= link_to '購入詳細一覧',orders_path,class:'nav-link active' %></li>
      </ul>
      <form class="d-flex">
        <% if request.path.include?("admin") %>
          <%= link_to "新規作成", new_admin_item_path,class: "btn btn-outline-primary"%>
        <% else %>
          <%= link_to carts_path, class: "btn btn-outline-dark" do  %>
            <i class="bi-cart-fill me-1"></i>
            カート
            <span class="badge bg-dark text-white ms-1 rounded-pill"><%= cart_item_size %></span>
          <% end %>
        <% end %>
      </form>
    </div>
  </div>
</nav>
